import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
  profile: {
    width: 80,
    height: 80,
    borderRadius: 40,
    marginBottom: 20,
    alignSelf: "center"
  }
});